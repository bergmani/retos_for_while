//Imprime repetidamente el valor de la variable xValue, dimsinuyéndolo en 0.5 cada vez, siempre que xValue siga siendo positivo.
let xValue = 5;
let disminuir = 0.5;

for(xValue; xValue >= 0; xValue -= 0.5){
    console.log(xValue);
};